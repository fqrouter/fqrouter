#!/bin/bash

# initialize environment variables
if [ -z `readlink $0` ] ; then
    BIN_DIR=$(cd `dirname $0` && pwd)
else
    BIN_DIR=$(dirname $(readlink $0))
fi

$BIN_DIR/fqrouter --debug smuggler $@